{
  "tasks": {
    "fmt": "deno run -A npm:standard --fix --global Deno --global WebSocket"
  },
  "fmt": {
    "files": {
      "DO NOT RUN 'deno fmt', RUN 'deno task fmt' INSTEAD! (deno lint is still useful)": true,
      "include": ["deno.json"],
      "exclude": ["lib/", "test/", "*.md"]
    }
  }
}
